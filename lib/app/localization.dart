import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';

/// Shared Material/Widgets localization configuration for the Indonesian UI.
const appLocalizationsDelegates = GlobalMaterialLocalizations.delegates;

const appSupportedLocales = <Locale>[Locale('id', 'ID'), Locale('en', 'US')];

const appDefaultLocale = Locale('id', 'ID');
